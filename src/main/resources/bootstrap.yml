spring:
  application:
    name: debit-card-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8100
  
management:
  endpoints:
    web:
      exposure:
        include: "*"

